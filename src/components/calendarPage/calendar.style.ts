import styled from "@emotion/styled";

export const Wrapper = styled.div`
  max-width: 1200px;
  margin: 40px auto;
  border: 1px dashed gray;
  border-radius: 2px;
`;

export const EventChecker = styled.ul`
  margin-left: 20px;
`;

export const EventList = styled.li`
  padding-bottom: 3px;
  border-bottom: 1px solid #eee;
  margin-bottom: 5px;
`;
